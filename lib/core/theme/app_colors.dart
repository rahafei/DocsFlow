import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color.fromRGBO(119, 81, 50, 1);
  static const Color secondary = Color.from(alpha: 1, red: 0.831, green: 0.827, blue: 0.812);
  static const Color background = Color(0xFFFEFDF9);
  static const Color text = Color.fromRGBO(77, 47, 23, 1);
  static const Color buttonText = Color(0xFFFFFFFF);
}